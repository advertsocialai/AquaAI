apiVersion: v1
kind: ConfigMap
metadata:
  name: aquai-config
  namespace: aquai
data:
  ENV: "prod"
  API_PREFIX: "/api/v1"
  REGION: "ap-south-1"
  TZ: "Asia/Kolkata"
  UPLOAD_DIR: "/data/uploads"
  CORS_ALLOWED_ORIGINS: "https://aquai.in,https://www.aquai.in"
  PRICE_TICK_INTERVAL_SECONDS: "60"
